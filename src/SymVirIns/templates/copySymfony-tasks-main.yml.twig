- name: Symfony | Copy files
  remote_user: vagrant
  command: rm -rf /home/vagrant/{{ project }}/vendor &&  cp -r /home/vagrant/{{ project }}/* /var/www/{{ project }} &&  cd /var/www/{{ project }} && composer update
  args:
    removes: /home/vagrant/{{ project }}/vendor
  tags: Symfony